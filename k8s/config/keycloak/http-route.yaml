apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-app-route
  namespace: demo-app
spec:
  parentRefs:
    - name: shared-gateway
      namespace: gateway-infra
  hostnames:
    - "auth.192.168.1.100.sslip.io"
  rules:
    - backendRefs:
        - name: fixme-svc # TODO change this
          port: 80
